# Place hosts in here and roles to run
---
- hosts: ansible
  roles:
    - standard_ssh
    - standard_update_packages
    - standard_patching
    - standard_cron
    - standard_iptables
    - ansible
    - ansible_git
    - ansible_playbooks
