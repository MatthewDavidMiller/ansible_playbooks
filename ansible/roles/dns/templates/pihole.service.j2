[Unit]
Description=Pod container-pihole.service
Wants=network.target
After=network-online.target

[Service]
Restart=on-failure
TimeoutStopSec=70
ExecStartPre=/usr/local/bin/kubectl delete -f "/home/{{ user_name }}/kubernetes/pihole.yml"
ExecStart=/usr/local/bin/kubectl create -f "/home/{{ user_name }}/kubernetes/pihole.yml"
ExecStop=/usr/local/bin/kubectl delete -f "/home/{{ user_name }}/kubernetes/pihole.yml"
Type=forking

[Install]
WantedBy=multi-user.target default.target
