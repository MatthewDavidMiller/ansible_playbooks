# Legacy Playbooks

These top-level playbooks were retired from the maintained workflow and moved under `archive/playbooks/`.

## Archived Playbooks

- `vpn.yml`
- `pihole.yml`
- `unificontroller.yml`
- `apcontroller.yml`
- `laptop_arch_install_1.yml`
- `laptop_arch_install_2.yml`
- `laptop_config.yml`

## Notes

- They are preserved for reference only.
- The active orchestrator chain remains at the repo root: `vm1.yml`, `homelab_vms.yml`, `update_homelab_vms.yml`, and `reboot_vms.yml`.
- Historical role implementations used by these playbooks live under `archive/roles/`.
