---
# nextcloud role defaults

# Path to the shared PostgreSQL 17 data directory (also used by paperless_ngx and semaphore)
postgres_path: ""

# Path to the Nextcloud container data directory (on the dedicated disk)
nextcloud_path: ""

# UID/GID used by Nextcloud to own files on the host-mounted data path
nextcloud_uid: "33"
nextcloud_gid: "33"

# Normalize permissions on all Nextcloud user file trees under data/<user>/files.
# Directories default to 0770; regular files default to 0660 so no execute bit is
# granted on file content while still keeping owner+group access only.
nextcloud_normalize_user_files_permissions: true
nextcloud_user_files_directory_mode: "0770"
nextcloud_user_files_file_mode: "0660"

# Block device identifier for the Nextcloud disk mount (e.g. "UUID=xxxx-xxxx")
nextcloud_disk: ""

# PostgreSQL admin username used to bootstrap service roles and databases
postgres_admin_user: ""

# Password for the PostgreSQL admin user
postgres_admin_password: ""

# Optional legacy PostgreSQL superuser to use once during migrations from an
# older cluster that was bootstrapped with a different admin role.
postgres_legacy_bootstrap_user: ""

# Password for the optional legacy PostgreSQL bootstrap user
postgres_legacy_bootstrap_password: ""

# PostgreSQL database name for Nextcloud
nextcloud_database_name: ""

# Nextcloud database username
nextcloud_db_user: ""

# Password for the Nextcloud database user
nextcloud_db_password: ""

# Nextcloud admin account username
nextcloud_admin_user: ""

# Nextcloud admin account password
nextcloud_admin_password: ""

# Space-separated list of trusted domains for Nextcloud
nextcloud_trusted_domains: ""

# Fully-qualified domain name used to build OVERWRITECLIURL
nextcloud_dns_name: ""

# Nextcloud username whose files directory is used for local backup destinations
nextcloud_homelab_user: ""

# Borg repository path for the Nextcloud + Paperless backup (on the local disk)
nextcloud_borg_backup_path: ""

# Rclone remote path for syncing the Borg repo (e.g. "Nextcloud:backup/nextcloud")
nextcloud_paperless_backup_location: ""

# Path to the Paperless NGX export directory (referenced in backup script)
paperless_export_path: ""

# PostgreSQL database name for Paperless NGX (used in db_wrapper.sh bootstrap)
paperless_database_name: ""

# PostgreSQL database name for Semaphore (used in db_wrapper.sh bootstrap)
semaphore_database_name: ""

# OS user that owns the shell scripts (must exist on the target host)
user_name: ""

# Root-only directory for runtime environment files
secret_env_dir: /etc/homelab/secrets

# Container images are resolved from the approved artifact lock in vm1.yml
postgres_image: ""
redis_image: ""
nextcloud_image: ""
