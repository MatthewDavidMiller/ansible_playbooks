#!/bin/bash
set -e
# POSTGRES_DB ({{ nextcloud_database_name }}) is created automatically by the POSTGRES_DB env var.
# Create the Paperless NGX database.
psql -U "$POSTGRES_USER" -c "CREATE DATABASE {{ paperless_database_name }};"
