---
# standard_selinux role defaults
# This role enforces SELinux policy, sets required booleans, and installs the
# custom podman_bpf module for Rocky Linux 10.

# Additional SELinux booleans for host-specific workflows.
standard_selinux_extra_booleans: []

# Additional SELinux file contexts for host-specific workflows.
standard_selinux_extra_fcontexts: []
