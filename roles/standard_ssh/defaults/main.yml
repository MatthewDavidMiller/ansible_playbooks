---
# standard_ssh role defaults
# This role applies hardened sshd_config settings using lineinfile.
# It uses no role-level variables — all settings are hardcoded per the project's
# SSH hardening policy.  This file is present so ansible-lint can confirm
# there are no undefined variables.
