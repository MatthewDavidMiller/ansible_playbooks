---
# standard_ssh role defaults
# Optional account allowlist for SSH. Leave empty to avoid locking out
# inventory-driven deployments that have not standardized on one login user.
standard_ssh_allow_users: []
