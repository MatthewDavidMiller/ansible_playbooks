# Place hosts in here and roles to run
---
- name: Configure vpn
  hosts: vpn
  roles:
    - standard_ssh
    - standard_qemu_guest_agent
    - standard_update_packages
    - configure_timezone
    - standard_cron
    - dynamic_dns
    - standard_firewalld
    - standard_podman
    - vpn
    - standard_cleanup
